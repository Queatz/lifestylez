<%= form_for @life_style, html: { multipart: true } do |f| %>
    <% if @life_style.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@life_style.errors.count, "error") %> prohibited this life_style from being saved:</h2>

          <ul>
            <% @life_style.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :user_id %><br>
      <%= f.number_field :user_id %>
    </div>
    <div class="field">
      <%= f.label :photo %>
      <%= f.file_field :photo %>
    </div>

    <div class="field">
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.label :description %><br>
      <%= f.text_area :description %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
